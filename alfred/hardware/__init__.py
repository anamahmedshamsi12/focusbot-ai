"""
alfred.hardware
-----------------
Real hardware drivers for Alfred's physical body: the OLED eyes
(alfred.hardware.oled) and the servo arm (alfred.hardware.servo). Each
driver falls back to a module in alfred.simulator when its hardware
isn't present.
"""
